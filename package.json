{
  "name": "lingarnew",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/lingar",
    "build": "npm run build --workspace=apps/lingar",
    "lint": "npm run lint --workspace=apps/lingar"
  }
}
